#pragma once

bool checkCollision(int firstX, int firstY, int firstWidth, int firstHeight, int secondX, int secondY, int secondWidth, int secondHeight);

// void addToArray(Upgrade upgrades[], Upgrade* upgrade, bool& found);